<h1><%= @cat.name %></h1>

<pre>
    <%= @cat.to_json %>
</pre>

<ul>
    <!-- @cat not @cats -->
    <% @cat.toys.each do |toy| %>
    <li><%= toy.name %></li>
    <% end %>
</ul>
<a href="<%= new_cat_toy_url(@cat) %>">New Toy</a>

<a href="<%= cats_url %>">Index</a>